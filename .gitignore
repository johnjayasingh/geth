# Per-network env (see config/networks/*/env.example)
.env.mainnet
.env.testnet

# Legacy single .env
.env
.env.local

# Validator keystores — never commit
validator-keys/**/*.json

# Runtime data
chaindata/mainnet/node*/
chaindata/testnet/node*/
tmp/*
!tmp/.gitkeep

# Old layout (remove after migrating)
chaindata/node*/

seed
scripts