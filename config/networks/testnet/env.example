# Copy to repo root as `.env.testnet` (or let setup-validator copy this automatically).
# Must match config/networks/testnet/genesis.json (replace that file if your testnet differs).

CHAINID=17172
BOOTNODE="enode://<testnet-bootnode>@<host>:32669"
# IP=
