# Copy to repo root as `.env.mainnet` (or let setup-validator copy this automatically).
# chainid must match config/networks/mainnet/genesis.json

CHAINID=17171
BOOTNODE="enode://<bootnode>@<host>:32669"
# Appended by setup-validator.sh if missing:
# IP=
