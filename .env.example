# Copy to `.env` before `./node-start.sh --validator`.
# node-setup.sh copies this to `.env` if missing, then appends your public IP.

CHAINID=17171
BOOTNODE="enode://<bootnode>@<host>:32669"
# IP is appended automatically by node-setup.sh; set manually if needed:
# IP=203.0.113.10
