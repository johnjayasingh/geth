# G8Chain uses per-network env files at the repo root:
#
#   .env.mainnet   — copy from config/networks/mainnet/env.example
#   .env.testnet   — copy from config/networks/testnet/env.example
#
# Scripts:  ./scripts/start-validator.sh --network mainnet|testnet --validator
